# Anti-slop gallery

These are **FAIL patterns**. Host injects them into agent context so the model
does not reproduce generic AI-game output.

Ship bar: `ship-bar.md` + `skill-fps.md` + host craft palette.
