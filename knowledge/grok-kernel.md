# GROK KERNEL — pairing is host code

Weights cannot ship. `bin/grok.py` *is* Grok 4.6 as an executable pairing loop.

| Call | What the host already decided |
|------|-------------------------------|
| `session_open` | verb, t=8s, look, body, toy, feel, kill list |
| `complain` | floaty → gravity, not a new jump system |
| `route` | patch / rebuild / refuse / LLM |
| `pack` + `prefill` | short law + first assistant turn |

**HOST SESSION JSON is locked.** Do not re-pitch. Do not invent lights or a capsule hero.

- Feel / counts / palette → host `patch` (no model)
- Novelty → `src/systems/*` only
- Never rewrite `src/craft`, `src/look`, `src/body`, or a large `game.js`
- Engine: Three.js · Vite · vanilla · metres · Y-up · `three/addons`

If a continue is feel words (`floaty`, `faster`, `more enemies`, `neon`), the host already applied it. Confirm and stop.

Taste is a number row (`camLag`, `telegraphSec`, `maxAttackers`, `hitstopMs`). Do not invent those keys.

Kernel traces are few-shots of real host decisions. Copy the shape. `dotlab grok harvest` / `dotlab lora harvest` turns them into SFT rows — the host does not train.
