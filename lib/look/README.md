# Look kit (immutable)

Host-owned Three.js place. The model picks a card id (`neon-night`, `pine-ridge`, …). It does not rewrite these files.

`applyLook({ scene, renderer, camera, pal, spec })` → lights, ground, landmark, InstancedMesh scatter, one shader.
