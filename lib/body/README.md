# Body kit (immutable)

Host-owned figures. The model picks card ids. It does not draw a green capsule.

`makePlayer` / `makeEnemy` / `makeWeapon` / `makeCover` + `tickPose`.
Feet at y=0. Metres. Y-up.
